# This script converts the result returned by objdump -M intel -d
# into Jasmin assembler code. 
# Requires Q language to be installed.
# We only intend to translate a subset of the binary program, 
# something that is enough to do matching with Furia-chan.
